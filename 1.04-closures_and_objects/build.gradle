task groovy{}

def foo = "1 milion dollars"
// a closure is a diferent way of declaring a function
def myClosure = {
    println "this is a closure"
    println "the value is $foo"
}

myClosure()

//it's a function that can be packed up and passed around variables

def var1 = myClosure
def var2 = var1
var2()

//closures have a diferent notation (it's similar to kotlin)

def tripleIt = {x -> x*3}

foo = "bar"
println "$foo + foo = ${foo + "foo"}"